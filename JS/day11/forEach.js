//forEach

const arrayNumbers = [10, 20, 30, 40, 50];

//forEach method is used to iterate over each element in an array

const forEachValue = arrayNumbers.forEach((value) => {
  console.log(value);
});
console.log("forEachValue", forEachValue);
